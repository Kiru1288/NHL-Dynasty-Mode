/**
 * NHL calendar date hooks / ISO ranges for firing events after sim advances.
 * Populate with real anchors when calendar integration exists.
 *
 * Scaffold only — no date literals or mock schedules here yet.
 */
export function getEventDateAnchors() {
  return {};
}
