import React from "react";

/**
 * Shared layout, focus trap, overlay, and header/footer slots for event modals.
 * Individual *Menu components compose inside this shell later.
 *
 * Scaffold only — pass-through wrapper.
 */
export default function EventModalShell({ children }) {
  return children ?? null;
}
